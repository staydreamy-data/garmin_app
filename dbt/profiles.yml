
garmin_training_log:
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', '../astro/include/data/duckdb/garmin_db.duckdb') }}"
      threads: 1
      schema: dev
  target: dev
