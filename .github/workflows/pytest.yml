name: pytest

on:
  push:
    branches-ignore:
      - "main"
    paths:
      - "**/*.py"
      - "dbt/**"
      - "tests/**"
      - "pyproject.toml"
      - "uv.lock"

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      AIRFLOW_HOME: ${{ github.workspace }}/astro
      AIRFLOW__CORE__DAGS_FOLDER: ${{ github.workspace }}/astro/dags
      PYTHONPATH: ${{ github.workspace }}/astro
      DBT_DUCKDB_PATH: ${{ github.workspace }}/.tmp/dbt/garmin_ci.duckdb

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra airflow-tests

      - name: Ruff check (fix)
        run: uv run ruff check --fix .

      - name: Ruff format
        run: uv run ruff format .

      - name: Run tests
        run: uv run pytest

      - name: Auto-commit Ruff fixes
        if: success()
        run: |
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add -A
            git commit -m "chore: apply Ruff fixes"
            git push
          else
            echo "No changes to commit."
          fi

      - name: Prepare dbt DuckDB path
        run: mkdir -p "$(dirname "$DBT_DUCKDB_PATH")"

      - name: dbt deps
        run: uv run dbt deps --project-dir dbt --profiles-dir dbt

      - name: dbt compile
        run: uv run dbt compile --project-dir dbt --profiles-dir dbt
